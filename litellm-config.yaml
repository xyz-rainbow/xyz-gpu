model_list:
  - model_name: hybrid-model
    litellm_params:
      model: openai/vllm
      api_base: http://localhost:8000/v1
      api_key: "any-key"
      rpm: 100
      tpm: 100000

router_settings:
  routing_strategy: failover
  allowed_fails: 2
  cooldown_time: 10
